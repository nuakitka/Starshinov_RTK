# backend1

Каталог сервиса `backend1`.

Контейнер поднимает только базовую Debian-среду и SSH-доступ для Ansible.
Установка `nginx`, публикация HTML-страницы и метрик выполняется Ansible-плейбуками.
